<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="10dp"
    android:paddingTop="20dp"
    android:paddingRight="10dp">

    <Button
        android:id="@+id/btn_unzip"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@color/purple_700"
        android:gravity="center"
        android:text="点击解压 ukbench 图片包"
        android:textColor="#ffffff"
        android:textSize="14sp" />

    <Button
        android:id="@+id/btn_batch"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="20dp"
        android:background="@color/purple_700"
        android:gravity="center"
        android:text="@string/activity_batch_parse"
        android:textColor="#ffffff"
        android:textSize="14sp"
        app:layout_constraintTop_toBottomOf="@+id/btn_unzip" />

    <Button
        android:id="@+id/btn_append"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="20dp"
        android:background="@color/purple_700"
        android:gravity="center"
        android:text="@string/activity_append_parse"
        android:textColor="#ffffff"
        android:textSize="14sp"
        app:layout_constraintTop_toBottomOf="@+id/btn_batch" />

</androidx.constraintlayout.widget.ConstraintLayout>